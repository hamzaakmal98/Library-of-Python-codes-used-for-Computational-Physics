import numpy as np

# create a NumPy array of 2000 elements, initialized to zero
rho = np.zeros(2000)

# set the values of rho[874] and rho[1135] to 0.3 and 0.7, respectively
rho[874] = 0.3
rho[1135] = 0.7
